version: '3.7'

services:
    php:
        container_name: random-string-generator-php
        image: random-string-generator-php
        restart: 'no'
        build:
            context: php
        volumes:
            - ./../:/var/app:cached
